{
  "name": "push_alerts_assistant_status",
  "slug": "push-alerts-assistant-status",
  "description": "Pub/sub push template: hourly, reads this tenant's own status and POSTs it to germany-internal's collect_tenant_status endpoint, labeled tenant 'Alerts-Assistant'.",
  "cron_schedules": [
    { "cron_expression": "0 * * * *", "enabled": true }
  ],
  "is_enabled": true,
  "timeout_ms": 30000
}
